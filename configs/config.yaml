model:
  backend: "qwen-local"
  model_path: "agentic_research_pipeline/Qwen1.5-0.5B-Chat"

llm_backend: "qwen"

pipeline:
  llm_verify: true
  max_bullets: 6
  remove_references: true
  ocr_if_empty: true
  export_pptx: false

paths:
  outputs_dir: "data/outputs"